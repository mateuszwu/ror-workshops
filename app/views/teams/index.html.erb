<h3>
  Teams
  <small class="text-muted"><%= link_to "New team", new_team_path, class: "btn btn-primary" %></small>
</h3>

<div id="teams" class="d-flex flex-row bd-highlight mb-3 gap-3">
  <% @teams.each do |team| %>
    <%= render team %>
  <% end %>
</div>
<%= link_to 'Edit Profile', edit_user_registration_path %>